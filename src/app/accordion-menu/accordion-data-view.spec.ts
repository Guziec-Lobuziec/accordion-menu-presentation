import { AccordionDataView } from './accordion-data-view';

describe('AccordionDataView', () => {
  it('should create an instance', () => {
    expect(new AccordionDataView()).toBeTruthy();
  });
});
